
(DONE) Make uniform the notation.
	  Prover9
		  there aren't a notation for nand ... nimpl


=== API ===

Unittest
  http://docs.python.org/library/unittest.html

== Propositional logic ==
Formula
	(DONE) self.connective = "not" instead self.connective = self.CONN["not"]
	(DONE) refactor alpha to is_alpha (idem beta)

Generalization
   trasform using __getitem__ __setitem__

(DONE) CNF

resolution or tableaux


== First Order Logic ==
Formula

CNF

Resolution or tableaux
		


=== GUI ===
(NO) Parser with antlr
Parser with ply

(DONE) Redirect parser's error output to gui.
Fix output redirect, too many \n
(DONE) vertical split: input and output | commands

Load the .ui file with PyQt4.uic.loadUi

Improve the quality of the icon


=== Parser ===
Manage comments % 

Propositional
  (DONE) check the "not" behaviour
  grammar for generalizations

First order
  ? insert equality